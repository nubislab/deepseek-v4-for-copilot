{
  "name": "deepseek-v4-bridge-local",
  "displayName": "DeepSeek V4 Bridge (Local Build)",
  "description": "Pick DeepSeek V4 Pro & Flash from the Copilot Chat model picker. Vision, thinking mode, agent tools — zero config, BYOK.",
  "icon": "resources/icon.png",
  "version": "0.3.1-local",
  "publisher": "Vizards",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vizards/deepseek-v4-for-copilot"
  },
  "engines": {
    "vscode": "^1.116.0",
    "node": ">=22"
  },
  "extensionDependencies": [
    "github.copilot-chat"
  ],
  "extensionKind": [
    "ui",
    "workspace"
  ],
  "categories": [
    "AI",
    "Chat",
    "Programming Languages"
  ],
  "keywords": [
    "deepseek",
    "deepseek v4",
    "deepseek-v4",
    "copilot",
    "github copilot",
    "copilot chat",
    "vision",
    "byok",
    "language model",
    "thinking",
    "reasoning",
    "ai",
    "chat"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "enabledApiProposals": [
    "languageModelThinkingPart"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "deepseek-v4-bridge",
        "displayName": "DeepSeek V4 Bridge",
        "managementCommand": "deepseek-v4-bridge.manage"
      }
    ],
    "commands": [
      {
        "command": "deepseek-v4-bridge.manage",
        "title": "DeepSeek V4 Bridge: Manage"
      },
      {
        "command": "deepseek-v4-bridge.setApiKey",
        "title": "DeepSeek V4 Bridge: Set API Key"
      },
      {
        "command": "deepseek-v4-bridge.getApiKey",
        "title": "DeepSeek V4 Bridge: Get API Key"
      },
      {
        "command": "deepseek-v4-bridge.clearApiKey",
        "title": "DeepSeek V4 Bridge: Clear API Key"
      },
      {
        "command": "deepseek-v4-bridge.setVisionModel",
        "title": "DeepSeek V4 Bridge: Set Vision Proxy Model"
      },
      {
        "command": "deepseek-v4-bridge.openSettings",
        "title": "DeepSeek V4 Bridge: Open Settings"
      },
      {
        "command": "deepseek-v4-bridge.showLogs",
        "title": "DeepSeek V4 Bridge: Show Logs"
      }
    ],
    "walkthroughs": [
      {
        "id": "deepseekV4BridgeGettingStarted",
        "title": "DeepSeek V4 Bridge for Copilot Chat",
        "description": "Set up DeepSeek V4 models in Copilot Chat.",
        "steps": [
          {
            "id": "setApiKey",
            "title": "Set your DeepSeek API key",
            "description": "[Get an API key](command:deepseek-v4-bridge.getApiKey) from deepseek.com.\n[Set API Key](command:deepseek-v4-bridge.setApiKey)",
            "media": {
              "markdown": "resources/walkthrough/set-api-key.md"
            },
            "completionEvents": [
              "onCommand:deepseek-v4-bridge.setApiKey"
            ]
          },
          {
            "id": "showModels",
            "title": "Show DeepSeek models",
            "description": "If the models are hidden, open VS Code's Language Models manager and show the DeepSeek models.\n[Open Language Models](command:workbench.action.chat.manage)",
            "media": {
              "markdown": "resources/walkthrough/show-models.md"
            }
          }
        ]
      }
    ],
    "configuration": {
      "title": "DeepSeek V4 Bridge",
      "properties": {
        "deepseek-v4-bridge.baseUrl": {
          "type": "string",
          "default": "https://api.deepseek.com",
          "description": "DeepSeek API base URL. Defaults to official DeepSeek API endpoint."
        },
        "deepseek-v4-bridge.maxTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 65536,
          "description": "Maximum number of output tokens per request. Set to 0 to use the DeepSeek V4 API default. Thinking tokens count toward this limit."
        },
        "deepseek-v4-bridge.apiKey": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Optional plaintext fallback for automation. Prefer **DeepSeek V4 Bridge: Set API Key**, which stores the key in VS Code SecretStorage."
        },
        "deepseek-v4-bridge.modelIdOverrides": {
          "type": "object",
          "default": {
            "deepseek-v4-flash": "deepseek-v4-flash",
            "deepseek-v4-pro": "deepseek-v4-pro"
          },
          "description": "Override the API model ID sent for each DeepSeek model. Defaults are prefilled with official DeepSeek IDs; change them only when using a compatible third-party API that uses different model names.",
          "additionalProperties": false,
          "properties": {
            "deepseek-v4-flash": {
              "type": "string",
              "description": "API model ID for DeepSeek V4 Flash"
            },
            "deepseek-v4-pro": {
              "type": "string",
              "description": "API model ID for DeepSeek V4 Pro"
            }
          }
        },
        "deepseek-v4-bridge.visionModel": {
          "type": "string",
          "default": "",
          "description": "Model ID for vision proxy (describes images for text-only DeepSeek models). Leave empty for auto-detection. Use 'DeepSeek V4 Bridge: Set Vision Proxy Model' command to pick one from a list."
        },
        "deepseek-v4-bridge.visionPrompt": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "Describe the visual contents of this image in detail, including any text, objects, people, or context that would be relevant for understanding it. Focus on factual visual elements.",
          "description": "Prompt sent to the vision proxy model when describing image attachments before forwarding them to DeepSeek."
        }
      }
    }
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('out', { recursive: true, force: true, maxRetries: process.platform === 'win32' ? 10 : 0 })\"",
    "vscode:prepublish": "npm run clean && npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "oxlint",
    "format": "oxfmt --write src/",
    "format:check": "oxfmt --check src/",
    "package": "node -e \"const fs=require('node:fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.mkdirSync('dist', { recursive: true })\" && vsce package -o dist/deepseek-v4-bridge-local.vsix",
    "publish": "vsce publish",
    "login": "vsce login Vizards"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.116.0",
    "@vscode/vsce": "^3.9.1",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "typescript": "^6.0.3"
  }
}
